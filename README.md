# HsepGan
Usage:
+ Train the model:

  python train.py --dataset xxx --cuda --direction x2x

+ Test the model:

  python test.py --dataset xxx --cuda
Dataset format:
+ train:
    -a
    -b
+ test:
    -a
    -b